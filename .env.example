PROJECT_ROOT_DIR="C:/Users/Stan/Node/MEC-map"
RELEASE_TEST_TARGET_DIR="C:/Users/Stan/Documents/Warcraft III/Maps/Maps/Ts/MecTest"
DEPLOY_TARGET_FILE="C:/Users/Stan/Documents/Warcraft III/Maps/Maps/Ts/Mec.w3x"

# Put the following to "true" only if you have prettier automatically triggered on saving
DISABLE_PRETTIER_STAGED_FILES="false"

# Platform can be "windows" or "linux"
PLATFORM="windows"

# Slide maps output directory for conversion with AI skill
MAPS_OUTPUT_DIR_FOR_AI_CONVERSION_TO_MEC=""

# MEC base map the slide map conversion starts from
MEC_BASE_MAP_LOCATION=""

# Warcraft III The Frozen Throne (Legacy) install, the folder holding War3.mpq and the other MPQs.
# Only needed to regenerate the list of the game's own file paths (yarn convert-slide-map:war3-paths), which the
# slide map conversion uses to find what a protected map replaces of the game. That list is in the repo, so a
# conversion runs without this.
WAR3_LEGACY_MPQS_LOCATION=""
